# Flood Risk Assessment and Groundwater Level Prediction System

the dataset imports/exports' paths are not set (moved them to folder after outputs)

run model_comparision.ipnyb alone in colab for T4 GPU